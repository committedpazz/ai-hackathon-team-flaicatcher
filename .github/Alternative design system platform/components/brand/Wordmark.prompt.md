The Cerios wordmark. **Type-only** — the official interlocking-star mark was not provided, so this renders "cerios" in plain type plus the "about software quality" tagline. Replace with the real SVG when available.

```jsx
<Wordmark />                       // on light
<Wordmark onDark tagline={false} />  // on the dark hero
```
